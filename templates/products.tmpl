{{define "products"}}
<ul class="grid">
{{range .Products}}
  <li class="card">
    <div class="img">
      <img src="{{.Image}}" alt="">
    </div>
    <h2>{{.Name}}</h2>
    <p>{{.Description}}</p>
  </li>
{{end}}
</ul>
{{end}}
